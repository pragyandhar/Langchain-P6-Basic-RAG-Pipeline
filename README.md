# Langchain-P6-Basic-RAG-Pipeline
You've already built all the individual pieces in Phase 1 — loading documents, splitting them, embedding them, storing them in a vector store. Project 6 is where you connect all of those pieces into a complete RAG pipeline and add the final missing piece — a proper retrieval chain that grounds the LLM's answers strictly in your documents.
